export default function Footer() {
  return (
    <footer className="footer">
      <p>
        Используемые технологии и концепции React: React, JSX, props, useState,
        useEffect композиция компонентов, условный рендеринг, методы массивов
        (map, filter), обработка событий.
      </p>
    </footer>
  );
}
